services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      # Mount model files without rebuilding the image.
      # Place yolov8n-face.pt (or the model set via YOLO_MODEL) here.
      - ./backend:/app
    restart: unless-stopped
